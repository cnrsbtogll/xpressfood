import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function ProductCard({image, productName, price}:any) {
  return (
    <View>
      <Text>ProductCard</Text>
    </View>
  )
}

const styles = StyleSheet.create({})